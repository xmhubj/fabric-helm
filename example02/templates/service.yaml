apiVersion: v1
kind: Service
metadata:
  name: {{ include "example02.fullname" . }}
  labels:
    app: {{ include "example02.name" . }}
    chart: {{ include "example02.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "example02.name" . }}
    release: {{ .Release.Name }}
